package com.ssafy.dib.ui.theme

import androidx.compose.ui.graphics.Color

/** DIB light palette. Navy carries trust, mint is the interactive accent, and coral is reserved for urgency. */
object WireframeColors {
    val Background = Color.White
    val Canvas = Color(0xFFF7F8FA)
    val Text = Color(0xFF191F28)
    val Muted = Color(0xFF6B7684)
    val Navy = Color(0xFF14294A)
    val NavySoft = Color(0xFFEFF3F8)
    val Border = Color(0xFFE8EBEF)
    val Surface = Color(0xFFF2F4F6)
    val Elevated = Color(0xFFFFFFFF)
    val Image = Surface
    val Subtle = Border
    val Search = Color(0xFFF2F4F6)
    val SearchBorder = Color(0xFFF2F4F6)
    val Mint = Color(0xFF6DDFC0)
    val MintSoft = Color(0xFFE8FAF5)
    val MintInk = Color(0xFF16745E)
    val Live = Color(0xFFF04452)
    val Favorite = Color(0xFFF04452)
    val UrgentBackground = Color(0xFFFFF0EA)
    val Urgent = Color(0xFFD9582F)
    val SuccessBackground = MintSoft
    val Success = MintInk
}
